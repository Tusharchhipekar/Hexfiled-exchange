# Port the websocket server listens on.
WSS_PORT=8080

# Redis instance the server subscribes to for market updates.
REDIS_URL=redis://localhost:6379

# Secret used to verify user JWTs. Must match http-backend's JWT_SECRET.
JWT_SECRET=exchange
