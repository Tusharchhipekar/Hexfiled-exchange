# Redis instance prices are published to.
REDIS_URL=redis://localhost:6379

# Postgres/TimescaleDB instance used to persist price data.
DATABASE_URL="postgresql://prisma:mysecret@localhost:5432/exchanges"

# Set to true to fall back to the REST price feed when the websocket feed is unavailable.
PRICE_FEEDER_REST_FALLBACK=false
