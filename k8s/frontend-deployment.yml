apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  labels:
    name: frontend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - image: frontend:latest
        imagePullPolicy: IfNotPresent
        name: frontend
        resources:
          limits:
            cpu: "250m"
            memory: "250Mi"
          requests:
            cpu: "100m"
            memory: "100Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 5173
          initialDelaySeconds: 20
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 5173
          initialDelaySeconds: 10
          timeoutSeconds: 10
        env:
        - name: VITE_WS_URL
          valueFrom:
            secretKeyRef:
              name: frontend
              key: VITE_WS_URL
        - name: VITE_API_URL
          valueFrom:
            secretKeyRef:
              name: frontend
              key: VITE_API_URL
        - name: API_PROXY_TARGET
          valueFrom:
            secretKeyRef:
              name: frontend
              key: API_PROXY_TARGET
        - name: WS_PROXY_TARGET
          valueFrom:
            secretKeyRef:
              name: frontend
              key: WS_PROXY_TARGET
        ports:
        - containerPort: 80
          name: frontend
